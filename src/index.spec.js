import React from 'react';
import ReactDOM from 'react-dom';

// Not actually testing right now.
it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<div />, div);
});
